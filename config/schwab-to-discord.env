# =============================================================================
# SCHWAB-TO-DISCORD CONFIGURATION
# =============================================================================
# This file contains NON-SECRET settings that are safe to commit to git.
# API keys and secrets go in: config/.env.secrets
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_NAME=Schwab to Discord
LOG_LEVEL=INFO

# =============================================================================
# SCHWAB POLLING SETTINGS
# =============================================================================
# How many days back to look for orders
TIME_DELTA_DAYS=7

# API request timeout in seconds
SCHWAB_TIMEOUT=10

# Filter for order status (FILLED, PENDING, etc.)
ORDER_STATUS=FILLED

# How often to poll Schwab for new orders (seconds)
POLL_INTERVAL_SECONDS=5

# =============================================================================
# DATABASE & FILE PATHS (Docker paths)
# =============================================================================
# These paths are inside the Docker container
# The /data folder is mounted as a volume for persistence

DB_PATH=/data/trades.db
TOKENS_DB=/data/tokens.db
EXPORT_PATH=/data/trades.xlsx

# =============================================================================
# DISCORD MESSAGE TEMPLATE (OPTIONAL)
# =============================================================================
# Customize how trade alerts appear in Discord
# Available placeholders:
#   {symbol}          - Ticker symbol (e.g., AAPL)
#   {strike}          - Strike price with C/P (e.g., 150c)
#   {expiration}      - Expiration date
#   {price}           - Fill price
#   {filled_quantity} - Contracts filled
#   {total_sold}      - Total contracts sold
#   {position_left}   - Remaining position
#   {gain_pct}        - Gain/loss percentage

TEMPLATE=**Option Bot**\nTicker: {symbol}\nStrike: {strike}\nExpiration: {expiration}\nEntry Price: {price}\nQuantity: {total_sold} sold | {filled_quantity} filled | {position_left} left to sell\nGain: {gain_pct}

# =============================================================================
# GOOGLE SHEETS EXPORT SCHEDULE (OPTIONAL)
# =============================================================================
# When to automatically export trades to Google Sheets
# Only runs if Google Sheets credentials are configured in .env.secrets

# Worksheet name in your spreadsheet
GOOGLE_SHEETS_WORKSHEET_NAME=Sheet1

# Day of week for export (mon, tue, wed, thu, fri, sat, sun)
GSHEET_EXPORT_DAY=sat

# Time of day for export (24-hour format)
GSHEET_EXPORT_HOUR=8
GSHEET_EXPORT_MINUTE=0
