# Schwab to Discord - Configuration
# =============================================================================
# This file contains NON-SECRET configuration that is safe to commit to git.
# Copy this to .env for your deployment.
#
# API keys and secrets should be in .env.secrets (use .env.secrets.example as template)
# =============================================================================

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_NAME="Schwab to Discord"

# =============================================================================
# SCHWAB API SETTINGS (non-secret)
# =============================================================================
# Number of days to look back for orders
TIME_DELTA_DAYS=3

# Callback URL for OAuth (usually localhost for local dev)
CALLBACK_URL="https://127.0.0.1"

# Request timeout in seconds
SCHWAB_TIMEOUT=10

# Order status filter (FILLED, PENDING, etc.)
ORDER_STATUS="FILLED"

# =============================================================================
# DATABASE & STORAGE PATHS
# =============================================================================
DB_PATH="/data/trades.db"
TOKENS_DB="/data/tokens.db"
EXPORT_PATH="/data/trades.xlsx"

# =============================================================================
# GOOGLE SHEETS EXPORT SETTINGS (non-secret)
# =============================================================================
# Path to Google service account credentials JSON
GOOGLE_SHEETS_CREDENTIALS_PATH="/data/credentials.json"

# Default worksheet name
GOOGLE_SHEETS_WORKSHEET_NAME="Sheet1"

# =============================================================================
# SCHEDULER SETTINGS
# =============================================================================
# Weekly export schedule (for nobelltrading.com Saturday posting)
GSHEET_EXPORT_DAY="sat"
GSHEET_EXPORT_HOUR=8
GSHEET_EXPORT_MINUTE=0

# =============================================================================
# DISCORD MESSAGE TEMPLATE (optional)
# =============================================================================
# Available placeholders:
#   {symbol} - Ticker symbol (e.g., ORCL)
#   {strike} - Strike price with C/P suffix (e.g., 149c, 267.5p)
#   {expiration} - Expiration date (e.g., 02/13/2026)
#   {price} - Current order price
#   {filled_quantity} - Number of contracts filled
#   {total_sold} - Total contracts sold for this symbol
#   {position_left} - Remaining position from Schwab
#   {gain_pct} - Percentage gain/loss for sells
#   {entry_price} - Original entry price for sells
#   {instruction} - BUY/SELL
#   {asset_type} - OPTION/EQUITY
#   {status} - Order status
#   {description} - Full option description
TEMPLATE="**Option Bot**\nTicker: {symbol}\nStrike: {strike}\nExpiration: {expiration}\nEntry Price: {price}\nQuantity: {total_sold} sold | {filled_quantity} filled | {position_left} left to sell\nGain: {gain_pct}"
